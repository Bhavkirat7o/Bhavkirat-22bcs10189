#include <iostream>

int main() {
    int n;
    std::cout << "Enter a positive integer: ";
    std::cin >> n;

    int sum = n * (n + 1) / 2;

    std::cout << "The sum of the first " << n << " natural numbers is: " << sum << std::endl;

    return 0;
}
